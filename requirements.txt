# Streamlit Cloud Dependencies
# This file contains ONLY the packages needed for deployment
# Why? Streamlit Cloud has limited resources, so we remove heavy/unnecessary packages

# Core framework (with specific versions that work on Python 3.11)
streamlit==1.31.0

# PDF processing
pymupdf==1.23.8

# Data manipulation
pandas==2.1.4
numpy==1.26.3

# Utilities
python-dotenv==1.0.0
requests==2.31.0
plotly==5.18.0

# Note: Removed qdrant-client, sentence-transformers, psycopg2-binary, sqlalchemy
# These have dependency conflicts and aren't needed for the demo version
# This is a simplified demo version - for full functionality deploy to AWS

# Note: We removed these because they're not available or needed on Streamlit Cloud:
# - fastapi (we're only running Streamlit, not FastAPI)
# - uvicorn (same reason)
# - openai (we're using Ollama locally)
# - mlflow (not needed for production)
# - faiss-cpu (we're using Qdrant)
